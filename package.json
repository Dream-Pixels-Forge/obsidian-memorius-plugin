{
  "name": "obsidian-memorius-vault",
  "version": "0.1.0",
  "description": "Vector memory vault for Obsidian — semantic search, context injection, cross-vault consolidation",
  "main": "src/main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": ["obsidian", "plugin", "memory", "vector-search", "memorius"],
  "author": "Dream Pixels Forge",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.7.0",
    "tslib": "^2.8.0",
    "typescript": "^5.7.0"
  }
}
